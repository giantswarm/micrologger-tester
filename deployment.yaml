apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: logger-test
  namespace: testing
  labels:
    app: logger-test
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: logger-test
    spec:
      containers:
      - name: logger-test 
        image: jgsqware/logger-test:1
       
      restartPolicy: Always